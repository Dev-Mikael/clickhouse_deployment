# This directory is managed automatically by FluxCD during bootstrap.
# Do NOT manually create or edit files here.
# Run: flux bootstrap github --owner=<YOUR_GITHUB_ORG> \
#        --repository=clickhouse-gitops \
#        --branch=main \
#        --path=clusters/production \
#        --personal
# FluxCD will create gotk-components.yaml, gotk-sync.yaml, and kustomization.yaml here.
